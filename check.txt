проверка four-slash write

Если выше "wrote check.txt" — значит проблема не в количестве слэшей, а в том что строка должна быть на столбце 1 с newline после маркера.

А если НЕ появилось — значит четыре слэша таки экранируют write!