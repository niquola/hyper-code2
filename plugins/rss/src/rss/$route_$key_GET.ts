/** GET /rss/:key — subscription details, recent entries and run history. */
export default async function(ctx:Context,_session:Session|null,opts:{req:Request;params:{key:string}}){const esc=(v:any)=>ctx.fns.procs.ui.escape({text:String(v??"")}),feed=await ctx.fns.rss.get({key:opts.params.key});if(!feed)return {status:404,main:"RSS feed not found"};const entries=feed.entries.map((e:any)=>`<div class="border-t border-base-200 px-4 py-3 first:border-t-0"><div class="flex gap-2"><a href="${esc(e.url)}" target="_blank" rel="noreferrer" class="min-w-0 flex-1 truncate text-sm font-medium hover:text-primary">${esc(e.title)}</a>${e.news_id?ctx.fns.procs.ui.badge({text:"in News",tone:"success"}):""}</div><div class="mt-1 text-[11px] text-base-content/40">${esc(e.published_at??e.last_seen_at)}</div></div>`).join("");const runs=feed.runs.map((r:any)=>`<div class="flex items-center gap-2 border-t border-base-200 py-2 first:border-t-0"><span class="font-mono text-[10px]">${esc(r.started_at)}</span><span class="ml-auto">${ctx.fns.procs.ui.badge({text:r.status,tone:r.status==="error"?"danger":"success"})}</span><span class="text-xs">${r.added} new · ${r.updated??0} changed · ${r.unchanged??0} same · ${r.published} published</span></div>`).join("");return {title:`RSS · ${feed.label}`,main:`<div class="mx-auto w-full max-w-5xl p-5 sm:p-8">${ctx.fns.procs.ui.breadcrumb({items:[{label:"RSS library",href:"/rss"},{label:feed.label}]})}<div class="mt-5 flex items-start justify-between gap-3"><div><h1 class="text-2xl font-semibold">${esc(feed.label)}</h1><a class="mt-1 block text-xs text-primary hover:underline" href="${esc(feed.url)}">${esc(feed.url)}</a></div>${ctx.fns.procs.ui.button({action:"load",label:"Load now",post:`/rss/${encodeURIComponent(feed.key)}/load`,target:"#rss-result",tone:"primary"})}</div><div id="rss-result" class="mt-4"></div><div class="mt-6 grid gap-5 lg:grid-cols-[minmax(0,1fr)_18rem]"><section class="overflow-hidden rounded-xl border border-base-300 bg-base-100"><div class="px-4 py-3 text-sm font-semibold">Recent entries</div>${entries||`<div class="p-6 text-sm text-base-content/45">No entries loaded</div>`}</section><aside class="rounded-xl border border-base-300 bg-base-100 p-4"><h2 class="mb-2 text-sm font-semibold">Load runs</h2>${runs||`<p class="text-xs text-base-content/40">No runs yet</p>`}</aside></div></div>`};}
