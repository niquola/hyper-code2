/** GET /news/share/picker — renders target buttons, a searchable destination combo and Send in one row. */
export default async function(ctx:Context,_session:Session|null,opts:{req:Request;params:Record<string,string>}){const u=new URL(opts.req.url),id=u.searchParams.get("id")??u.searchParams.get("share-id")??"",target=u.searchParams.get("target")??"",selected=u.searchParams.get("destination")??"",esc=(v:any)=>ctx.fns.procs.ui.escape({text:String(v??"")});if(!id||!target)return "";const [targets,destinations,item]=await Promise.all([ctx.fns.news.shareTargets({}),ctx.fns.news.shareDestinations({target}),ctx.fns.news.get({id})]),destination=destinations.find((d:any)=>String(d.id)===selected),shared=selected&&!!item?.reposts?.[`${target}:${selected}`],tabs=targets.map((t:any)=>`<button type="button" class="ui-button ui-button--sm ${t.target===target?"ui-button--primary":"ui-button--default"}" hx-get="/news/share/picker?id=${encodeURIComponent(id)}&target=${encodeURIComponent(t.target)}" hx-target="#reader-share-picker" hx-swap="innerHTML"><i class="ph ${esc(t.icon)}"></i><span>${esc(t.label)}</span></button>`).join("");return `<div><form hx-post="/news/share" hx-target="#reader-share-result" hx-swap="innerHTML" class="flex h-9 items-center gap-2 overflow-x-auto"><span class="shrink-0 text-xs font-semibold text-base-content/45">Share to</span><input type="hidden" name="id" value="${esc(id)}"><input type="hidden" name="target" value="${esc(target)}"><input type="hidden" name="destination" value="${esc(selected)}"><div class="flex shrink-0 gap-1">${tabs}</div><label class="relative min-w-72 flex-1"><span class="pointer-events-none absolute inset-y-0 left-0 flex w-10 items-center justify-center"><i class="ph ${destination?"ph-check-circle text-success":"ph-magnifying-glass text-base-content/35"} leading-none"></i></span><input name="q" value="${esc(destination?.label??"")}" autocomplete="off" placeholder="Find ${esc(targets.find((t:any)=>t.target===target)?.label??target)} channel" class="h-9 w-full rounded-lg border ${destination?"border-success":"border-base-300"} bg-base-100 pl-10 pr-3 text-sm leading-9 outline-none focus:border-primary" hx-get="/news/share/destinations" hx-trigger="focus, input changed delay:200ms, search" hx-target="#share-destination-results" hx-include="[name='id'],[name='target']"></label><button class="ui-button ui-button--sm ui-button--primary shrink-0" ${!selected||shared?"disabled":""} onclick="return confirm('Share this post to ${esc(destination?.label??"the selected destination")}?')"><i class="ph ph-paper-plane-tilt"></i><span>${shared?"Shared":"Send"}</span></button></form><div id="share-destination-results" class="mt-1 max-h-56 overflow-auto"></div></div>`;}
